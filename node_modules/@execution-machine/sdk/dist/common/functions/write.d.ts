import { WriteAction, WriteOpResult } from "../model";
export declare const writeFunction: <T = any>(functionId: string, writeOps: Array<WriteAction> | WriteAction, emToken: string) => Promise<WriteOpResult<T>>;
